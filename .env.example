# ReimburseMe - Environment Variables
# The app runs fully locally — no configuration is required.
#
# OPTIONAL: AI receipt scanning via Claude. Copy this file to .env.local and
# fill in your key (https://console.anthropic.com). Without a key, receipt
# scanning falls back to the built-in offline OCR — everything still works.
ANTHROPIC_API_KEY=your-key-here
